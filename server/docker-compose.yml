version: "2"
services:
  node:
    image: "markdown-editor"
    build:
      context: .
    environment:
      - NODE_ENV=development
    volumes:
      - ./:/usr/src/app
    ports:
      - "8080:8080"
    command: "npm run dev"
